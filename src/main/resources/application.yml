spring:
  datasource:
    url: jdbc:mysql://49.51.244.170:3306/ai_doc_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&autoReconnect=true&tcpKeepAlive=true
    username: 
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      # connection-timeout: 30000
      # idle-timeout: 600000
      # maximum-pool-size: 10
      # minimum-idle: 5
      max-lifetime: 1200000 # 20分钟强制换一次连接
      connection-test-query: SELECT 1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  port: 8080

app:
  upload:
    dir: uploads

ai:
  api-key: ""
  api-url: "https://api.deepseek.com/chat/completions"
